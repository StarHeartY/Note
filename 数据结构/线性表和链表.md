# 第二章 课堂练习

1. 在一个单链表中 P 所指结点后插入一个 S 所指结点时，然后交换这两个结点的数据域，可执行如下操作：  
    (1) S->next = ① ;  
    (2) P->next = S;  
    (3) t = P->data;  
    (4) P->data = ② ;  
    (5) S->data = ③ ;
    
2. 在一个单链表中删除 P 所指结点时，应执行以下操作：  
    q = P->next;  
    P->data = P->next->data;  
    P->next = ______ ;  
    free(q);
    
3. 对于一个具有 n 个结点的单链表，在给定值为 x 的结点后插入一个新结点的时间复杂度是：______ ;
    
4. 若频繁地对一个线性表进行插入和删除操作，则该线性表宜采用何种存储结构？
    
5. 设计算法：从顺序表中删除所有值为 x 的元素。
    
    
 ```c
    
    Void delall(SqList &L) {
        int i = 0, j;
        while (i < L.length && L.elem[i] != x) i++;
        for (j = i + 1; j < L.length; j++)
            if (L.elem[j] != x) {
                L.elem[i] = L.elem[j];
                i++;
            }
        L.length = i;
    }
```

---

# 第三章 课堂练习

1. 一个栈的进栈序列是 a, b, c, d, e，则栈的不可能的输出序列是：  
    A. edcba  
    B. decba  
    C. dceab  
    D. abcde
    
2. 若已知一个栈的进栈序列是 1, 2, 3, …, 其输出序列为 P₁, P₂, P₃, …, Pₙ。若 Pₙ = n，则 Pᵢ (1 ≤ i ≤ n) 为：  
    A. i  
    B. n - i  
    C. n - i + 1  
    D. 不确定
    
3. 若已知一个栈的进栈序列是 1, 2, 3, …, 其输出序列为 P₁, P₂, P₃, …, Pₙ。若 P₁ = 3，则 P₂ 为：  
    A. 可能是 2  
    B. 一定不是 2  
    C. 可能是 1  
    D. 一定是 1
    
4. 向一个链头指针为 HS 的链栈中插入一个 S 所指结点时，则执行：  
    A. HS->next = S;  
    B. S->next = HS->next; HS->next = S;  
    C. S->next = HS; HS = S;  
    D. S->next = HS; HS = HS->next;
    
5. 循环队列中是否可以插入下一个元素：  
    A. 与队头指针和队尾指针的值有关  
    B. 只与队尾指针的值有关，与队头指针的值无关  
    C. 只与数组大小有关，与队首指针和队尾指针的值无关  
    D. 与曾经进行过多少次插入操作有关
    
6. 在一个链队中，假设 f 和 r 分别为队头和队尾指针，则插入 s 所指结点的运算是：  
    A. f->next = s; f = s  
    B. r->next = s; r = s  
    C. s->next = r; r = s  
    D. s->next = f; f = s
    
7. 循环队列 DeQueue(Q, e) 操作删除元素 e，则下列 ______ 是队首 front 的正确移动位置。  
    A. Q->front + 1  
    B. Q->base + Q->front  
    C. Q->front + MAXQSIZE  
    D. (Q->front + 1) % MAXQSIZE
    
8. 算法设计：编写一个算法，利用栈的基本运算返回指定栈中栈底元素。
    
    c
    
    Elemtype bottom(Stack S) {
        ElemType x, y;
        stack tempS;
        InitStack(tempS);
        while (!StackEmpty(S)) {
            pop(S, x);
            push(tempS, x);
        }
        while (!StackEmpty(tempS)) {
            pop(tempS, y);
            push(S, y);
        }
        return x;
    }
    

---

# 第四章 课堂练习

1. 串是一种特殊的线性表，其特殊性体现在：  
    A. 可以顺序存储  
    B. 数据元素是一个字符  
    C. 可以链接存储  
    D. 数据元素可以是多个
    
2. 设有两个串 p 和 q，求 q 在 p 中首次出现的位置的运算称作：  
    A. 连接  
    B. 模式匹配  
    C. 求子串  
    D. 求串长
    
3. 若串 s = “software”，其非空子串的个数是：  
    A. 8  
    B. 37  
    C. 36  
    D. 9
    
4. 空串是 ______ ，其长度等于 ______ 。  
    空白串是 ______ ，其长度等于 ______ 。
    
5. 两个串相等的充分必要条件是 ______ 。
    

---

# 第五章 课堂练习

1. 一维数组和线性表的区别是：  
    A. 前者长度固定，后者长度可变  
    B. 后者长度固定，前者长度可变  
    C. 两者长度固定  
    D. 两者长度均可变
    
2. 二维数组 M 的元素是 4 个字符组成的串（每个字符占一个存储单元）；下标 i 的范围从 0 到 4，列下标 j 的范围从 0 到 5。M 按行存储时元素 M[3][5] 的起始地址与 M 按列存储时元素 ______ 的起始地址相同。  
    A. M[2][4]  
    B. M[3][4]  
    C. M[3][5]  
    D. M[4][4]
    
3. 设有一个 10 阶的对称矩阵 A，采用压缩存储方式，以行序为主存储，a₁₁ 为第一个元素，其存储地址为 1，每个元素占 1 个地址空间，则 a₇₅ 的地址为：  
    A. 25  
    B. 26  
    C. 27  
    D. 40
    
4. 稀疏矩阵一般的压缩存储方法有两种，即：  
    A. 二维数组和三维数组  
    B. 三元组和散列  
    C. 三元组和十字链表  
    D. 散列和十字链表
    
5. 一个广义表的表头总是一个广义表，这个断言是：  
    A. 正确的  
    B. 错误的
    
6. 一个广义表的表尾总是一个广义表，这个断言是：  
    A. 正确的  
    B. 错误的
    
7. 已知广义表 L = (((x, y, z), a), (u, t, w))，从 L 表中取出原子 t 的运算是：  
    A. head[tail[tail[L]]]  
    B. tail[head[head[head[tail[L]]]]]  
    C. head[tail[head[tail[L]]]]  
    D. head[head[tail[tail[L]]]]
    

---

# 第六章 课堂练习

1. 在线索化二叉树中，t 所指结点没有左子树的充要条件是：  
    A. t->lchild == NULL  
    B. t->ltag == 1  
    C. t->ltag == 1, t->lchild == NULL  
    D. 以上都不对
    
2. 二叉树的先序遍历序列中，任意一个结点均处在其孩子结点的前面，这种说法：  
    A. 正确  
    B. 错误
    
3. 如果 T₂ 是由树 T₁ 转换而来的二叉树，那 T₁ 中结点的先根序列就是 T₂ 中结点的：  
    A. 先序  
    B. 中序  
    C. 后序  
    D. 层次序
    
4. 如果 T₂ 是由树 T₁ 转换而来的二叉树，那 T₁ 中结点的后根序列就是 T₂ 中结点的：  
    A. 先序  
    B. 中序  
    C. 后序  
    D. 层次序
    
5. 某二叉树的先序遍历序列和后序遍历序列正好相反，则该二叉树一定是：  
    A. 空或只有一个结点  
    B. 完全二叉树  
    C. 二叉树  
    D. 高度等于其结点数
    
6. 深度为 5 的二叉树至多有 ______ 个结点：  
    A. 16  
    B. 32  
    C. 31  
    D. 10
    
7. 对于一个满二叉树，m 个树叶，n 个结点，深度为 h，则：  
    A. n = h + m  
    B. h + m = 2n  
    C. m = h - 1  
    D. n = 2ʰ - 1
    
8. 设 n, m 为一棵二叉树上两个结点，在中序遍历时，n 在 m 前的条件是：  
    A. n 在 m 右下方  
    B. n 是 m 祖先  
    C. n 在 m 左下方  
    D. n 是 m 子孙
    
9. 线索二叉树是一种 ______ 结构：  
    A. 逻辑  
    B. 逻辑和存储  
    C. 物理  
    D. 线性
    
10. 根据使用频率，为 5 个字符设计的赫夫曼编码，不可能的解是：  
    A. 111, 110, 10, 01, 00  
    B. 000, 001, 010, 011, 1  
    C. 100, 11, 10, 1, 0  
    D. 001, 000, 01, 11, 10
    
11. 已知一棵二叉树的中序序列为 cbedahgijf，后序序列为 cedbhjigfa，画出该二叉树。
    
12. 二叉树采用链接存储结构，试设计一个层次顺序（同一层次自左至右）遍历二叉树的算法：
    
    c
    
    Void translevel(BiTree T) {
        if (T != NULL) {
            printf(T->data);
            enqueue(Q, T);
            while (!QueueEmpty(Q)) {
                Dequeue(Q, P);
                if (P->lchild != NULL) {
                    printf(P->lchild->data);
                    enqueue(Q, P->lchild);
                }
                if (P->rchild != NULL) {
                    printf(P->rchild->data);
                    enqueue(Q, P->rchild);
                }
            }
        }
    }
    

---

# 第七章 课堂练习

1. 在一个图中，所有顶点的度数之和等于所有边数的 ______ 倍：  
    A. ½  
    B. 1  
    C. 2  
    D. 4
    
2. 具有 4 个顶点的无向完全图有 ______ 条边：  
    A. 6  
    B. 12  
    C. 16  
    D. 20
    
3. 对于一个具有 n 个顶点和 e 条边的无向图，若采用邻接矩阵表示，则该矩阵的大小是：  
    A. n  
    B. (n-1)*(n-1)  
    C. n-1  
    D. n²
    
4. 对于一个具有 n 个顶点和 e 条边的无向图，若采用邻接表表示，则表头向量的大小为 ① ；所有邻接表中的结点总数是 ② 。  
    ① A. n B. n+1 C. n-1 D. n+e  
    ② A. e/2 B. e C. 2e D. n+e
    
5. 对于某无向图的邻接矩阵来说：  
    A. 第 i 行上的非零元素和第 i 列的非零元素个数一定相等  
    B. 矩阵中的非零元素个数等于图中的边数  
    C. 第 i 行上，第 i 列上非零元素总数等于 Vᵢ 的度数  
    D. 矩阵中非全零行的行数等于图中的顶点数
    
6. 采用邻接表存储的图的深度优先遍历算法类似于二叉树的：  
    A. 先序遍历  
    B. 中序遍历  
    C. 后序遍历  
    D. 按层遍历
    
7. 判定一个有向图是否存在回路除了可以利用拓扑排序方法外，还可以用 ______ 方法。
    
8. 下面不正确的说法是：  
    A. 关键活动不按期完成就会影响整个工程的完成时间  
    B. 任何一个关键活动提前完成，将使整个工程提前完成  
    C. 所有关键活动都提前完成，则整个工程提前完成  
    D. 某些关键活动若提前完成，将使整个工程提前完成
    
9. 在 n 个顶点的无向图中，若边数 > n-1，则该图必是连通图。此断言是 ______ 的。
    
10. 一个图的 ______ 表示法是唯一的，而 ______ 表示法是不唯一的。
    
11. 已知一个图的邻接矩阵表示，删除所有从第 i 个结点出发的边的方法是 ______。
    
12. 图的深度优先搜索和广度优先搜索序列不是唯一的，此断言是 ______ 的。
    

---

# 第九章 课堂练习

1. 对线性表进行折半查找时，要求线性表必须 ______。  
    A. 以顺序方式存储  
    B. 以链接方式存储  
    C. 以顺序方式存储，且结点按关键字有序排序  
    D. 以链接方式存储，且结点按关键字有序排序
    
2. 采用顺序查找法查找长度为 n 的线性表时，每个元素的平均查找长度为：  
    A. n  
    B. n/2  
    C. (n+1)/2  
    D. (n-1)/2
    
3. 如果要求一个线性表既能较快地查找，又能适应动态变化的要求，可采用 ______ 查找方法：  
    A. 分块  
    B. 顺序  
    C. 折半  
    D. 散列
    
4. 一棵深度为 k 的平衡二叉树，其每个非终端结点的平衡因子平均为 0，则该树共有 ______ 个结点：  
    A. 2ᵏ⁻¹ - 1  
    B. 2ᵏ⁻¹  
    C. 2ᵏ⁻¹ + 1  
    D. 2ᵏ - 1
    
5. 下面关于 B- 树和 B+ 树的叙述中，不正确的结论是：  
    A. 有 n 棵子树的 B- 树和 B+ 树结点中都含有 n 个关键字  
    B. B- 树和 B+ 树都能有效地支持查找  
    C. B- 树和 B+ 树都是平衡的多叉树  
    D. B- 树和 B+ 树都可用于文件索引结构
    
6. 设散列表长 m=14，散列函数 H(key)=key%11。表中已有 4 个结点：  
    addr(15)=4, addr(38)=5, addr(61)=6, addr(84)=7，其余地址为空。如用二次探测再散列处理冲突，关键字为 49 结点的地址是：  
    A. 8  
    B. 3  
    C. 5  
    D. 9
    
7. 在各种方法中，平均查找长度与结点个数 n 无关的查找方法是 ______。
    
8. 在 n 个记录的有序顺序表中进行折半查找，最大的比较次数是 ______。
    
9. 折半查找的存储结构仅限于 ______，且是 ______。
    
10. 一个无序序列可以通过构造一棵 ______ 变成一个有序序列，构造树的过程即为对无序序列进行排序的过程。
    
11. 用二叉排序树查找，在最坏情况下，平均查找长度为 ______；当二叉排序树是一棵平衡二叉树时，ASL 平均查找长度为 ______。
    

---

# 第十章 课堂练习

1. 排序方法中，从未排序列中依次取出元素与已排序序列（初始时为空）中的元素进行比较，将其放入已排序序列的正确位置上的方法称为：  
    A. 希尔排序  
    B. 冒泡排序  
    C. 插入排序  
    D. 选择排序
    
2. 在待排序的元素序列基本有序的前提下，效率最高的排序方法是：  
    A. 插入排序  
    B. 选择排序  
    C. 快速排序  
    D. 归并排序
    
3. 对给出的一组关键字 {14, 5, 19, 20, 11, 19}。若按关键字非递减排序，第一趟排序结果为 {14, 5, 19, 20, 11, 19}。问采用的排序方法是：  
    A. 简单选择排序  
    B. 快速排序  
    C. 希尔排序  
    D. 二路归并排序
    
4. 快速排序方法在 ______ 情况下最不利于发挥其长处：  
    A. 要排序的数据量太大  
    B. 要排序的数据中含有多个相同值  
    C. 要排序的数据已基本有序  
    D. 要排序的数据个数为奇数
    
5. 如果对 n 个元素进行直接选择排序，则进行任一趟排序的过程中，为“寻找最小元素”所需要的时间复杂度为：  
    A. O(1)  
    B. O(log₂n)  
    C. O(n²)  
    D. O(n)
    
6. 在对 n 个元素的序列进行排序时，堆排序所需要的附加存储空间是：  
    A. O(log₂n)  
    B. O(1)  
    C. O(n)  
    D. O(nlog₂n)
    
7. 设有 n 个元素组成的无序序列，希望挑选出其中前 k 个（k<<n）最小元素，以下几种排序方法中 ______ 最高效：  
    A. 简单选择排序  
    B. 归并排序  
    C. 冒泡排序  
    D. 堆排序
    
8. 就排序算法所用的辅助空间而言，堆排序、快速排序、归并排序的关系是：  
    A. 堆排序 < 快速排序 < 归并排序  
    B. 堆排序 < 归并排序 < 快速排序  
    C. 堆排序 > 归并排序 > 快速排序  
    D. 堆排序 > 快速排序 > 归并排序
    
9. ______ 排序方法采用的是二分法的思想，______ 排序方法其数据的组织采用完全二叉树结构。
    
10. 有 n 个不同的英文单词，它们的长度相等，均为 m，若 n>>50，m<5，试问采用什么排序方法时间复杂度最佳？